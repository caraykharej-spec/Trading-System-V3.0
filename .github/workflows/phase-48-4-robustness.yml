name: Phase 48.4 Robustness Sampling

on:
  pull_request:
    branches: [main]
  push:
    branches: [main, "phase-48-4-*"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  robustness:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install -e ".[dev]"
      - run: python -m pytest -q tests/backtest/test_phase_48_4_robustness_sampling.py
